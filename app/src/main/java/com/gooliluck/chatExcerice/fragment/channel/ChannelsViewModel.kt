package com.gooliluck.chatExcerice.fragment.channel

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class ChannelsViewModel(application: Application) : AndroidViewModel(application) {
}